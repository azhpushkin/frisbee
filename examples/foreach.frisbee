fun void main() {
    [Int] x = [1, 1, 2, 3, 5, 8];
    
    println("First is " + x[0].to_string());
    println("Second is " + x[1].to_string());
    println("Third is " + x[2].to_string());
    println("");

    foreach i in range(1, x.len() - 1) {
        println("Item " + i.to_string() + " is " + x[i].to_string());
    }
    return;
}


/* EXPECTED STDOUT
==========
First is 1
Second is 1
Third is 2

Item 1 is 1
Item 2 is 2
Item 3 is 3
Item 4 is 5
========== */
