class Person {
    String name;
    Int age;

    fun String name_extended(String prefix) {
        @age = 213;
        return prefix + " " + @name;
    }
}

fun void main() {
    Person p = Person("Anton", 100500);
    println(p.name_extended("Mr."));
    return;
}
