active A {
    fun A(Int q) {
        println("Spawned new A with " + q.to_string());
    }

    fun void say_hello(String name) {
        println("Hello, " + name);
    }
}

fun void main() {
    A a1 = spawn A(1);
    A a2 = spawn A(2);
    // a ! say_hello("Stranger");
}


/* EXPECTED STDOUT
==========
Hello, Stranger
========== */

