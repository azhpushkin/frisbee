active A {
    fun A(String name) {
        println("Spawned new A for " + name);
    }

    fun void say_hello(String name) {
        println("Hello, " + name);
    }
}

fun void main() {
    A a1 = spawn A("Alice");
    A a2 = spawn A("Bob");
    // a ! say_hello("Stranger");
}


/* EXPECTED STDOUT
==========
Hello, Stranger
========== */

