from socket import SocketServer;


active Main {
   def Void run() {
     val s = spawn SocketServer(9999);

     while (true) {
       val x <= s ! get();
       io ! print("Received", x);
     }

   }
}