from socket import SocketServer;


active Main {
   def Void run() {
     val s = spawn SocketServer(9999);
     #val v <= io ! readline();#
     val p <= io ! print(void);
     io ! print(p);

     val x <= s ! get();
   }
}